// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osdblockchain/osdblockchain/v1/storage_subscription.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "osdblockchain.osdblockchain.v1";

/**
 * StorageSubscription represents an active or historical storage subscription
 * purchased by a user. This is a higher–level abstraction over individual
 * purchase events and can be used to enforce storage quotas, durations, and
 * usage.
 */
export interface StorageSubscription {
  /**
   * id is a unique identifier for this subscription (scoped to a buyer).
   * The keeper is responsible for generating IDs (e.g. using a sequence).
   */
  id: string;
  /** buyer is the address that owns this subscription. */
  buyer: string;
  /** start_time is when the subscription becomes active (Unix timestamp, seconds). */
  startTime: number;
  /** end_time is when the subscription expires (Unix timestamp, seconds). */
  endTime: number;
  /** space_available is the maximum number of bytes available to this subscription. */
  spaceAvailable: number;
  /** space_used is how many bytes have been consumed so far. */
  spaceUsed: number;
  /**
   * credits is a generic quota metric (e.g. bytes * duration in seconds),
   * represented as a string so it can map cleanly to a Cosmos SDK math.Int.
   */
  credits: string;
}

function createBaseStorageSubscription(): StorageSubscription {
  return { id: "", buyer: "", startTime: 0, endTime: 0, spaceAvailable: 0, spaceUsed: 0, credits: "" };
}

export const StorageSubscription: MessageFns<StorageSubscription> = {
  encode(message: StorageSubscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.buyer !== "") {
      writer.uint32(18).string(message.buyer);
    }
    if (message.startTime !== 0) {
      writer.uint32(24).int64(message.startTime);
    }
    if (message.endTime !== 0) {
      writer.uint32(32).int64(message.endTime);
    }
    if (message.spaceAvailable !== 0) {
      writer.uint32(40).int64(message.spaceAvailable);
    }
    if (message.spaceUsed !== 0) {
      writer.uint32(48).int64(message.spaceUsed);
    }
    if (message.credits !== "") {
      writer.uint32(58).string(message.credits);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageSubscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.buyer = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.startTime = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.endTime = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.spaceAvailable = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.spaceUsed = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.credits = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageSubscription {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      buyer: isSet(object.buyer) ? globalThis.String(object.buyer) : "",
      startTime: isSet(object.startTime) ? globalThis.Number(object.startTime) : 0,
      endTime: isSet(object.endTime) ? globalThis.Number(object.endTime) : 0,
      spaceAvailable: isSet(object.spaceAvailable) ? globalThis.Number(object.spaceAvailable) : 0,
      spaceUsed: isSet(object.spaceUsed) ? globalThis.Number(object.spaceUsed) : 0,
      credits: isSet(object.credits) ? globalThis.String(object.credits) : "",
    };
  },

  toJSON(message: StorageSubscription): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.buyer !== "") {
      obj.buyer = message.buyer;
    }
    if (message.startTime !== 0) {
      obj.startTime = Math.round(message.startTime);
    }
    if (message.endTime !== 0) {
      obj.endTime = Math.round(message.endTime);
    }
    if (message.spaceAvailable !== 0) {
      obj.spaceAvailable = Math.round(message.spaceAvailable);
    }
    if (message.spaceUsed !== 0) {
      obj.spaceUsed = Math.round(message.spaceUsed);
    }
    if (message.credits !== "") {
      obj.credits = message.credits;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageSubscription>, I>>(base?: I): StorageSubscription {
    return StorageSubscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageSubscription>, I>>(object: I): StorageSubscription {
    const message = createBaseStorageSubscription();
    message.id = object.id ?? "";
    message.buyer = object.buyer ?? "";
    message.startTime = object.startTime ?? 0;
    message.endTime = object.endTime ?? 0;
    message.spaceAvailable = object.spaceAvailable ?? 0;
    message.spaceUsed = object.spaceUsed ?? 0;
    message.credits = object.credits ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
