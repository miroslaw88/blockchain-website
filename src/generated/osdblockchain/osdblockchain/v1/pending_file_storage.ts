// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osdblockchain/osdblockchain/v1/pending_file_storage.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "osdblockchain.osdblockchain.v1";

/**
 * PendingFileStorage represents a file that is waiting for sufficient storage providers
 * to become available to meet its max_proofs requirement.
 */
export interface PendingFileStorage {
  /** merkle_root is the Merkle root hash of the file chunks (used as the file identifier). */
  merkleRoot: string;
  /** owner is the address that owns this file. */
  owner: string;
  /** size_bytes is the size of the file in bytes. */
  sizeBytes: number;
  /** required_providers is the number of storage providers needed (max_proofs from the original request). */
  requiredProviders: number;
  /** expiration_time is when the file storage expires (Unix timestamp, seconds). */
  expirationTime: number;
  /** posted_at is when the file was posted (Unix timestamp, seconds). */
  postedAt: number;
}

function createBasePendingFileStorage(): PendingFileStorage {
  return { merkleRoot: "", owner: "", sizeBytes: 0, requiredProviders: 0, expirationTime: 0, postedAt: 0 };
}

export const PendingFileStorage: MessageFns<PendingFileStorage> = {
  encode(message: PendingFileStorage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.merkleRoot !== "") {
      writer.uint32(10).string(message.merkleRoot);
    }
    if (message.owner !== "") {
      writer.uint32(18).string(message.owner);
    }
    if (message.sizeBytes !== 0) {
      writer.uint32(24).int64(message.sizeBytes);
    }
    if (message.requiredProviders !== 0) {
      writer.uint32(32).int64(message.requiredProviders);
    }
    if (message.expirationTime !== 0) {
      writer.uint32(40).int64(message.expirationTime);
    }
    if (message.postedAt !== 0) {
      writer.uint32(48).int64(message.postedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PendingFileStorage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePendingFileStorage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.merkleRoot = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.owner = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sizeBytes = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.requiredProviders = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.expirationTime = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.postedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PendingFileStorage {
    return {
      merkleRoot: isSet(object.merkleRoot) ? globalThis.String(object.merkleRoot) : "",
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      sizeBytes: isSet(object.sizeBytes) ? globalThis.Number(object.sizeBytes) : 0,
      requiredProviders: isSet(object.requiredProviders) ? globalThis.Number(object.requiredProviders) : 0,
      expirationTime: isSet(object.expirationTime) ? globalThis.Number(object.expirationTime) : 0,
      postedAt: isSet(object.postedAt) ? globalThis.Number(object.postedAt) : 0,
    };
  },

  toJSON(message: PendingFileStorage): unknown {
    const obj: any = {};
    if (message.merkleRoot !== "") {
      obj.merkleRoot = message.merkleRoot;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.sizeBytes !== 0) {
      obj.sizeBytes = Math.round(message.sizeBytes);
    }
    if (message.requiredProviders !== 0) {
      obj.requiredProviders = Math.round(message.requiredProviders);
    }
    if (message.expirationTime !== 0) {
      obj.expirationTime = Math.round(message.expirationTime);
    }
    if (message.postedAt !== 0) {
      obj.postedAt = Math.round(message.postedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PendingFileStorage>, I>>(base?: I): PendingFileStorage {
    return PendingFileStorage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PendingFileStorage>, I>>(object: I): PendingFileStorage {
    const message = createBasePendingFileStorage();
    message.merkleRoot = object.merkleRoot ?? "";
    message.owner = object.owner ?? "";
    message.sizeBytes = object.sizeBytes ?? 0;
    message.requiredProviders = object.requiredProviders ?? 0;
    message.expirationTime = object.expirationTime ?? 0;
    message.postedAt = object.postedAt ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
