// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osdblockchain/osdblockchain/v1/indexer.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "osdblockchain.osdblockchain.v1";

/**
 * IndexerRange defines the hash prefix range assigned to an indexer.
 * This supports both range-based assignments (for initial group-based distribution)
 * and exact prefix assignments.
 */
export interface IndexerRange {
  /** indexer_id is the unique identifier for the indexer */
  indexerId: string;
  /** indexer_address is the network address of the indexer (e.g., "ip:port" or domain) */
  indexerAddress: string;
  /**
   * hash_prefix_start is the start of the prefix range this indexer handles (e.g., "AB", "AB12", "AB1234")
   * For range-based assignments, this is the start of the range
   */
  hashPrefixStart: string;
  /**
   * prefix_depth is the number of hex digits in the prefix (2, 4, 6, 8, etc.)
   * 2 = 1 byte (256 possible prefixes), 4 = 2 bytes (65K possible), etc.
   */
  prefixDepth: number;
  /**
   * hash_prefix_end is the end of the range (only used for range-based assignments)
   * For exact prefix assignments, this equals hash_prefix_start
   */
  hashPrefixEnd: string;
  /** is_exact_prefix indicates if this is an exact prefix match (true) or a range (false) */
  isExactPrefix: boolean;
  /** group_id is the replication group this indexer belongs to (0, 1, 2, ...) */
  groupId: number;
  /** file_count is the number of files indexed in this range (for load tracking) */
  fileCount: number;
  /** query_count is the number of queries handled (optional, for load balancing) */
  queryCount: number;
  /** registered_at is the block height when this indexer was registered */
  registeredAt: number;
  /** last_updated is the block height when this range was last updated */
  lastUpdated: number;
  /** is_active indicates if this indexer is currently active */
  isActive: boolean;
}

/**
 * PrefixLoad tracks the load (file count) for a specific prefix.
 * This is used for monitoring and load tracking purposes.
 */
export interface PrefixLoad {
  /** prefix is the hash prefix (e.g., "AB", "AB12") */
  prefix: string;
  /** depth is the prefix depth (number of hex digits) */
  depth: number;
  /** file_count is the number of files with this prefix */
  fileCount: number;
  /** query_count is the number of queries for this prefix (optional) */
  queryCount: number;
  /** last_updated is the block height when this was last updated */
  lastUpdated: number;
}

function createBaseIndexerRange(): IndexerRange {
  return {
    indexerId: "",
    indexerAddress: "",
    hashPrefixStart: "",
    prefixDepth: 0,
    hashPrefixEnd: "",
    isExactPrefix: false,
    groupId: 0,
    fileCount: 0,
    queryCount: 0,
    registeredAt: 0,
    lastUpdated: 0,
    isActive: false,
  };
}

export const IndexerRange: MessageFns<IndexerRange> = {
  encode(message: IndexerRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.indexerId !== "") {
      writer.uint32(10).string(message.indexerId);
    }
    if (message.indexerAddress !== "") {
      writer.uint32(18).string(message.indexerAddress);
    }
    if (message.hashPrefixStart !== "") {
      writer.uint32(26).string(message.hashPrefixStart);
    }
    if (message.prefixDepth !== 0) {
      writer.uint32(32).int32(message.prefixDepth);
    }
    if (message.hashPrefixEnd !== "") {
      writer.uint32(42).string(message.hashPrefixEnd);
    }
    if (message.isExactPrefix !== false) {
      writer.uint32(48).bool(message.isExactPrefix);
    }
    if (message.groupId !== 0) {
      writer.uint32(56).int64(message.groupId);
    }
    if (message.fileCount !== 0) {
      writer.uint32(64).int64(message.fileCount);
    }
    if (message.queryCount !== 0) {
      writer.uint32(72).int64(message.queryCount);
    }
    if (message.registeredAt !== 0) {
      writer.uint32(80).int64(message.registeredAt);
    }
    if (message.lastUpdated !== 0) {
      writer.uint32(88).int64(message.lastUpdated);
    }
    if (message.isActive !== false) {
      writer.uint32(96).bool(message.isActive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexerRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIndexerRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.indexerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.indexerAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.hashPrefixStart = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.prefixDepth = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.hashPrefixEnd = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.isExactPrefix = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.groupId = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.fileCount = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.queryCount = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.registeredAt = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.lastUpdated = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IndexerRange {
    return {
      indexerId: isSet(object.indexerId) ? globalThis.String(object.indexerId) : "",
      indexerAddress: isSet(object.indexerAddress) ? globalThis.String(object.indexerAddress) : "",
      hashPrefixStart: isSet(object.hashPrefixStart) ? globalThis.String(object.hashPrefixStart) : "",
      prefixDepth: isSet(object.prefixDepth) ? globalThis.Number(object.prefixDepth) : 0,
      hashPrefixEnd: isSet(object.hashPrefixEnd) ? globalThis.String(object.hashPrefixEnd) : "",
      isExactPrefix: isSet(object.isExactPrefix) ? globalThis.Boolean(object.isExactPrefix) : false,
      groupId: isSet(object.groupId) ? globalThis.Number(object.groupId) : 0,
      fileCount: isSet(object.fileCount) ? globalThis.Number(object.fileCount) : 0,
      queryCount: isSet(object.queryCount) ? globalThis.Number(object.queryCount) : 0,
      registeredAt: isSet(object.registeredAt) ? globalThis.Number(object.registeredAt) : 0,
      lastUpdated: isSet(object.lastUpdated) ? globalThis.Number(object.lastUpdated) : 0,
      isActive: isSet(object.isActive) ? globalThis.Boolean(object.isActive) : false,
    };
  },

  toJSON(message: IndexerRange): unknown {
    const obj: any = {};
    if (message.indexerId !== "") {
      obj.indexerId = message.indexerId;
    }
    if (message.indexerAddress !== "") {
      obj.indexerAddress = message.indexerAddress;
    }
    if (message.hashPrefixStart !== "") {
      obj.hashPrefixStart = message.hashPrefixStart;
    }
    if (message.prefixDepth !== 0) {
      obj.prefixDepth = Math.round(message.prefixDepth);
    }
    if (message.hashPrefixEnd !== "") {
      obj.hashPrefixEnd = message.hashPrefixEnd;
    }
    if (message.isExactPrefix !== false) {
      obj.isExactPrefix = message.isExactPrefix;
    }
    if (message.groupId !== 0) {
      obj.groupId = Math.round(message.groupId);
    }
    if (message.fileCount !== 0) {
      obj.fileCount = Math.round(message.fileCount);
    }
    if (message.queryCount !== 0) {
      obj.queryCount = Math.round(message.queryCount);
    }
    if (message.registeredAt !== 0) {
      obj.registeredAt = Math.round(message.registeredAt);
    }
    if (message.lastUpdated !== 0) {
      obj.lastUpdated = Math.round(message.lastUpdated);
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IndexerRange>, I>>(base?: I): IndexerRange {
    return IndexerRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexerRange>, I>>(object: I): IndexerRange {
    const message = createBaseIndexerRange();
    message.indexerId = object.indexerId ?? "";
    message.indexerAddress = object.indexerAddress ?? "";
    message.hashPrefixStart = object.hashPrefixStart ?? "";
    message.prefixDepth = object.prefixDepth ?? 0;
    message.hashPrefixEnd = object.hashPrefixEnd ?? "";
    message.isExactPrefix = object.isExactPrefix ?? false;
    message.groupId = object.groupId ?? 0;
    message.fileCount = object.fileCount ?? 0;
    message.queryCount = object.queryCount ?? 0;
    message.registeredAt = object.registeredAt ?? 0;
    message.lastUpdated = object.lastUpdated ?? 0;
    message.isActive = object.isActive ?? false;
    return message;
  },
};

function createBasePrefixLoad(): PrefixLoad {
  return { prefix: "", depth: 0, fileCount: 0, queryCount: 0, lastUpdated: 0 };
}

export const PrefixLoad: MessageFns<PrefixLoad> = {
  encode(message: PrefixLoad, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== "") {
      writer.uint32(10).string(message.prefix);
    }
    if (message.depth !== 0) {
      writer.uint32(16).int32(message.depth);
    }
    if (message.fileCount !== 0) {
      writer.uint32(24).int64(message.fileCount);
    }
    if (message.queryCount !== 0) {
      writer.uint32(32).int64(message.queryCount);
    }
    if (message.lastUpdated !== 0) {
      writer.uint32(40).int64(message.lastUpdated);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrefixLoad {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrefixLoad();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.depth = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.fileCount = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.queryCount = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.lastUpdated = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrefixLoad {
    return {
      prefix: isSet(object.prefix) ? globalThis.String(object.prefix) : "",
      depth: isSet(object.depth) ? globalThis.Number(object.depth) : 0,
      fileCount: isSet(object.fileCount) ? globalThis.Number(object.fileCount) : 0,
      queryCount: isSet(object.queryCount) ? globalThis.Number(object.queryCount) : 0,
      lastUpdated: isSet(object.lastUpdated) ? globalThis.Number(object.lastUpdated) : 0,
    };
  },

  toJSON(message: PrefixLoad): unknown {
    const obj: any = {};
    if (message.prefix !== "") {
      obj.prefix = message.prefix;
    }
    if (message.depth !== 0) {
      obj.depth = Math.round(message.depth);
    }
    if (message.fileCount !== 0) {
      obj.fileCount = Math.round(message.fileCount);
    }
    if (message.queryCount !== 0) {
      obj.queryCount = Math.round(message.queryCount);
    }
    if (message.lastUpdated !== 0) {
      obj.lastUpdated = Math.round(message.lastUpdated);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrefixLoad>, I>>(base?: I): PrefixLoad {
    return PrefixLoad.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrefixLoad>, I>>(object: I): PrefixLoad {
    const message = createBasePrefixLoad();
    message.prefix = object.prefix ?? "";
    message.depth = object.depth ?? 0;
    message.fileCount = object.fileCount ?? 0;
    message.queryCount = object.queryCount ?? 0;
    message.lastUpdated = object.lastUpdated ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
