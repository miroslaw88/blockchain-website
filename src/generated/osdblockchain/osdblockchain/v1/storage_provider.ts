// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: osdblockchain/osdblockchain/v1/storage_provider.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "osdblockchain.osdblockchain.v1";

/**
 * StorageProvider defines a registered storage provider.
 * Storage providers store actual file data off-chain and can submit proofs
 * to the blockchain to verify they are storing files correctly.
 */
export interface StorageProvider {
  /** provider_id is the unique identifier for the storage provider */
  providerId: string;
  /** provider_address is the network address of the storage provider (e.g., "ip:port" or domain) */
  providerAddress: string;
  /** registered_at is the block height when this provider was registered */
  registeredAt: number;
  /** last_updated is the block height when this provider was last updated */
  lastUpdated: number;
  /** is_active indicates if this provider is currently active */
  isActive: boolean;
  /** total_capacity_bytes is the total storage capacity advertised by this provider (in bytes) */
  totalCapacityBytes: number;
  /** used_capacity_bytes is the amount of storage currently used (in bytes) */
  usedCapacityBytes: number;
}

function createBaseStorageProvider(): StorageProvider {
  return {
    providerId: "",
    providerAddress: "",
    registeredAt: 0,
    lastUpdated: 0,
    isActive: false,
    totalCapacityBytes: 0,
    usedCapacityBytes: 0,
  };
}

export const StorageProvider: MessageFns<StorageProvider> = {
  encode(message: StorageProvider, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.providerId !== "") {
      writer.uint32(10).string(message.providerId);
    }
    if (message.providerAddress !== "") {
      writer.uint32(18).string(message.providerAddress);
    }
    if (message.registeredAt !== 0) {
      writer.uint32(24).int64(message.registeredAt);
    }
    if (message.lastUpdated !== 0) {
      writer.uint32(32).int64(message.lastUpdated);
    }
    if (message.isActive !== false) {
      writer.uint32(40).bool(message.isActive);
    }
    if (message.totalCapacityBytes !== 0) {
      writer.uint32(48).int64(message.totalCapacityBytes);
    }
    if (message.usedCapacityBytes !== 0) {
      writer.uint32(56).int64(message.usedCapacityBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StorageProvider {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStorageProvider();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.providerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.providerAddress = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.registeredAt = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.lastUpdated = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.totalCapacityBytes = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.usedCapacityBytes = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StorageProvider {
    return {
      providerId: isSet(object.providerId) ? globalThis.String(object.providerId) : "",
      providerAddress: isSet(object.providerAddress) ? globalThis.String(object.providerAddress) : "",
      registeredAt: isSet(object.registeredAt) ? globalThis.Number(object.registeredAt) : 0,
      lastUpdated: isSet(object.lastUpdated) ? globalThis.Number(object.lastUpdated) : 0,
      isActive: isSet(object.isActive) ? globalThis.Boolean(object.isActive) : false,
      totalCapacityBytes: isSet(object.totalCapacityBytes) ? globalThis.Number(object.totalCapacityBytes) : 0,
      usedCapacityBytes: isSet(object.usedCapacityBytes) ? globalThis.Number(object.usedCapacityBytes) : 0,
    };
  },

  toJSON(message: StorageProvider): unknown {
    const obj: any = {};
    if (message.providerId !== "") {
      obj.providerId = message.providerId;
    }
    if (message.providerAddress !== "") {
      obj.providerAddress = message.providerAddress;
    }
    if (message.registeredAt !== 0) {
      obj.registeredAt = Math.round(message.registeredAt);
    }
    if (message.lastUpdated !== 0) {
      obj.lastUpdated = Math.round(message.lastUpdated);
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.totalCapacityBytes !== 0) {
      obj.totalCapacityBytes = Math.round(message.totalCapacityBytes);
    }
    if (message.usedCapacityBytes !== 0) {
      obj.usedCapacityBytes = Math.round(message.usedCapacityBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StorageProvider>, I>>(base?: I): StorageProvider {
    return StorageProvider.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StorageProvider>, I>>(object: I): StorageProvider {
    const message = createBaseStorageProvider();
    message.providerId = object.providerId ?? "";
    message.providerAddress = object.providerAddress ?? "";
    message.registeredAt = object.registeredAt ?? 0;
    message.lastUpdated = object.lastUpdated ?? 0;
    message.isActive = object.isActive ?? false;
    message.totalCapacityBytes = object.totalCapacityBytes ?? 0;
    message.usedCapacityBytes = object.usedCapacityBytes ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
